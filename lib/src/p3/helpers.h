#pragma once

/*
namespace Pitri
{
	template<class T>
	class Vec2
	{
		public:
			Vec2() = default;
			Vec2(T x, T y);
			T x, y;
	};

	template<class T>
	class Vec3
	{
		public:
			Vec3() = default;
			Vec3(T x, T y, T z);
			T x, y, z;
	};

	template<class T>
	class Rect
	{
		public:
			Rect() = default;
			Rect(T x, T y, T wdt, T hgt);
			Rect(Vec2<T> pos, Vec2<T> size);
			Vec2<T> pos;
			Vec2<T> size;
	};
}

template<class T>
Pitri::Vec2<T> operator+(const Pitri::Vec2<T> &left, const Pitri::Vec2<T> &right);

*/